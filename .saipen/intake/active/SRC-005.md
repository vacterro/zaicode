# User request

priority: P1
verify: the requested change is present and demonstrated against the user own description of it

## Request

ZAICODE

MODE
AUDIT RECONCILIATION + AUTONOMOUS IMPLEMENTATION CONTINUATION

AUTHORITY

Continue from the supplied archive state.

This handoff supersedes narrower ZAICODE continuation instructions, while preserving all valid requirements from SRC-001 through SRC-004.

Work sequentially through all eligible milestones without asking the operator to continue.

After each bounded slice:
1. verify focused behavior;
2. inspect adjacent regression risk;
3. update SAIPEN state/evidence through the repository protocol;
4. continue automatically to the next eligible milestone.

Before context exhaustion:
- checkpoint exact state;
- record verification already completed;
- record unresolved risks;
- record the next exact action;
- preserve enough evidence for cold recovery.

Stop only for a genuine external/operator blocker.

CURRENT AUDIT STATE

The archive reports:

- `.saipen/STATE.md`: phase DONE, task none.
- T-11 DONE.
- T-9 BLOCKED on interactive desktop E2E.
- T-10 BLOCKED on operator policy for LLM-initiated self-delegation.
- ZAICODE account-independent product mode exists.
- persistent agent definitions exist.
- durable work queue exists.
- cockpit UI exists.
- direct provider/model execution exists.
- standalone identity primitives exist.
- routing abstraction exists.
- 13/13 focused ZAICODE tests were reported green in the stored log.
- typecheck/lint/web build/desktop build were reported green in the stored log.

However, the current snapshot contains several material reconciliation gaps and implementation defects. Do not accept the current `DONE` state blindly.

AUDIT FINDING 1 — SOURCE / ACCEPTANCE EVIDENCE DRIFT
SEVERITY: P0 reconciliation integrity

`.saipen/LOG.md` explicitly claims that these files were created and used as acceptance evidence:

- `UI.md`
- `docs/ZAICODE_ARCHITECTURE.md`
- `docs/ZAICODE_IMPLEMENTATION.md`

Examples:
- LOG E-9 claims `docs/ZAICODE_ARCHITECTURE.md` was written.
- LOG E-22 claims `docs/ZAICODE_IMPLEMENTATION.md` and `UI.md` were written.
- LOG E-84 claims root `UI.md` exists and contains the required invariants.
- LOG E-120/E-122 again claims UI.md and architecture documentation were updated and verified.

But the supplied audit snapshot does not contain those files.

The snapshot does contain:
- `docs/ZAICODE_BASELINE_RECEIPT.md`
- `docs/ZAICODE_ACCOUNT_INDEPENDENCE_VALIDATION.md`
- `docs/ZAICODE_UPSTREAM_DELTA.md`

The missing files are also not visible as ordinary included files in the current audit representation.

This creates a source-of-truth contradiction:
SAIPEN acceptance says the artifacts exist, while the current project snapshot does not carry them.

MILESTONE 0 — RECONCILE PROJECT TRUTH FIRST

Before adding features:

1. Run the normal SAIPEN recovery/continue entrypoint and inspect the actual live tree.
2. Determine whether:
   A. the files exist in the live project but AUDAPACK omitted them, or
   B. the files are genuinely absent from the live project.

If A:
- do not recreate or rewrite them unnecessarily;
- record an explicit audit-representation provenance defect;
- preserve ZAICODE source state;
- classify the packaging omission separately from ZAICODE implementation;
- make sure future audit snapshots include the authoritative product/architecture contracts.

If B:
- the previous DONE receipt is not fully reproducible;
- reopen/create the smallest protocol-valid corrective work item;
- reconstruct the missing documents from SRC-001..SRC-004 and current implementation;
- do not invent historical evidence;
- describe current behavior, not intended behavior, wherever they differ.

Do not leave STATE=DONE while mandatory acceptance artifacts are genuinely absent.

AUDIT FINDING 2 — AGENT TOOL POLICY IS PERSISTED BUT NOT ENFORCED
SEVERITY: P0 behavioral integrity

`packages/shared/src/zaicode-agents.ts` defines:

- `toolPolicy.allowedTools`
- `toolPolicy.disallowedTools`
- `toolPolicy.permissionMode`

The definitions are persisted through the ZAICODE agent repository/service.

But `packages/desktop/src/host/zaicodeRunDispatch.ts` only maps:

- `permissionMode === "plan"` -> runtime mode `plan`

The executor does not project:

- `allowedTools` into the runtime tool allowlist;
- `disallowedTools` into the runtime tool denylist.

The upstream runtime already supports tool restriction fields. Existing source contains `toolAllowlist` / `toolDenylist` handling in the ZCode task/agent path.

Therefore the current ZAICODE agent contract can represent restrictions that the actual run does not enforce.

This is unacceptable because configuration must not imply a capability/security boundary that runtime ignores.

MILESTONE 1 — MAKE TOOL POLICY TRUE

Implement one consistent policy.

Preferred direction:
- preserve ZAICODE agent `allowedTools` / `disallowedTools`;
- translate them through the existing upstream task/runtime tool restriction mechanism;
- do not invent another permission engine.

Requirements:

1. `allowedTools`, when configured, must constrain the actual execution tool surface.
2. `disallowedTools` must remove the specified tools from the actual execution tool surface.
3. Existing runtime safety restrictions must not be weakened by ZAICODE policy.
4. Define deterministic behavior when allowlist and denylist conflict.
5. `plan` mode must continue to map to the real runtime permission mode.
6. Empty/undefined lists must preserve normal upstream behavior.
7. Persisted policy and actual runtime policy must be inspectably related.
8. Add focused tests proving that the executor sends the correct runtime restrictions.
9. Do not claim enforcement based only on schema persistence or UI state.

If an upstream runtime boundary makes an advertised policy impossible to enforce, remove that unsupported field from the product contract instead of leaving a decorative control.

AUDIT FINDING 3 — ROUTING BACKEND ABSTRACTION IS NOT REACHABLE FROM AGENT CONFIGURATION
SEVERITY: P1 architecture

`packages/shared/src/zaicode-routing.ts` declares:

- `direct`
- `saifren`
- `sairoute`
- `9router`

and correctly fails closed for unimplemented backends.

But the actual agent definition in `packages/shared/src/zaicode-agents.ts` has no routing-backend field.

`createZaicodeJobExecutor()` calls `resolveZaicodRoutePlan()` with:
- agentId
- role
- configuredSelection
- providerRef
- modelRef

It does not provide `backend`.

Therefore the resolver always defaults to `direct`.

The SAIFREN / SAIRoute / 9router backends currently exist only as unreachable declarations/tests. The architecture seam is not yet connected to persisted operator intent.

MILESTONE 2 — MAKE THE ROUTING SEAM REAL WITHOUT IMPLEMENTING EXTERNAL ROUTERS

Introduce the smallest persisted route-policy selector needed to choose the routing boundary.

For example, an agent route configuration may contain an explicit backend discriminator, but choose the naming/schema that best matches the existing architecture.

Requirements:

1. Existing agents migrate/default safely to `direct`.
2. The persisted schema validates the backend.
3. Agent create/update paths preserve it.
4. The executor passes it into `resolveZaicodRoutePlan`.
5. `direct` continues using the existing provider/model execution path.
6. `saifren`, `sairoute`, and `9router` remain fail-closed until real contracts are implemented.
7. Selecting an unimplemented backend must produce the existing truthful `route-unresolved` classification.
8. Do not fabricate fallback execution through another backend.
9. Do not implement SAIFREN/SAIRoute/9router network protocols in this wave.
10. UI only needs enough exposure to inspect/configure the persisted backend cleanly; do not build a giant routing dashboard.

Add tests proving:
- default -> direct;
- persisted direct -> executable when modelSelection is valid;
- persisted unimplemented backend -> route-unresolved;
- executor actually uses the persisted backend rather than silently defaulting.

AUDIT FINDING 4 — ZAICODE IDENTITY EXISTS, BUT THERE IS NO CANONICAL ZAICODE BUILD LANE
SEVERITY: P1 release/product separation

`packages/desktop/scripts/desktop-product-identity.mjs` now correctly defines:

- flavor `zaicode`
- appId `dev.zaicode.app`
- productName `ZAICODE`
- executable/package identity `zaicode`
- strict `ZCODE_ZAICODE_IDENTITY=1`

This is a useful primitive.

But the normal project entrypoints still contain no dedicated ZAICODE build command.

Root `package.json` has ordinary ZCode commands such as:
- `dev:desktop`
- `dev:desktop:test`
- `dev:desktop:prod`
- `bundle:desktop`

There is no canonical:
- ZAICODE development command;
- ZAICODE build command;
- ZAICODE package command.

`tools/start-zaicode-dev.ps1` currently sets:
- `ZCODE_ZAICODE_MODE=1`

but does not set:
- `ZCODE_ZAICODE_IDENTITY=1`

and then launches:
- `pnpm.cmd dev:desktop:test`

This means product capability mode and desktop identity can drift apart.

The stored log demonstrates that a desktop build succeeds when the identity environment variable is manually supplied, but that is not yet a safe product build path.

MILESTONE 3 — CREATE ONE CANONICAL ZAICODE DEVELOPMENT / BUILD / PACKAGE PATH

Create a narrow ZAICODE-owned wrapper or scripts around the existing upstream build machinery.

Do not fork the whole ZCode build system.

Required invariant:

A developer/operator building ZAICODE through the documented ZAICODE entrypoint must not be able to accidentally produce a ZCode or ZCode Preview identity because one environment variable was forgotten.

Canonical ZAICODE entrypoint must centrally establish both:

- ZAICODE product behavior;
- ZAICODE desktop identity.

At minimum ensure the effective environment includes the equivalent of:

- `ZCODE_ZAICODE_MODE=1`
- `ZCODE_ZAICODE_IDENTITY=1`

and the intended backend environment separately.

Prefer one reusable Node build-env wrapper with thin PowerShell convenience if appropriate, rather than duplicating build logic.

Update `tools/start-zaicode-dev.ps1` to consume the same authoritative ZAICODE environment definition if practical.

Provide discoverable root commands such as the repository convention-equivalent of:

- dev ZAICODE
- build ZAICODE
- bundle/package ZAICODE

Exact script names are implementation-owned.

FAIL CLOSED

Add a build-time invariant so a ZAICODE package cannot be produced with:
- ZAICODE product mode true but non-ZAICODE product flavor;
- ZAICODE product flavor true but ZAICODE capability mode missing where that distinction would expose normal ZCode product UX.

Do not rely on human memory to set two independent switches forever. Humans already invented environment variables, which was punishment enough.

VERIFY IDENTITY

Mechanically verify at least:
- resolved product flavor = zaicode;
- productName = ZAICODE;
- appId = dev.zaicode.app;
- package/executable identity = zaicode;
- product mode is enabled;
- production/preview identities remain unchanged.

If packaging can be run safely in the environment, run the Windows x64 package path.

If full packaging is unavailable because of external requirements, verify the build/configuration resolution mechanically and leave the final installed-app check explicit rather than claiming it passed.

MILESTONE 4 — PRODUCT CONTRACT / DOCUMENTATION RECONCILIATION

After runtime behavior is corrected:

Ensure the live project contains and accurately updates:

- `UI.md`
- `docs/ZAICODE_ARCHITECTURE.md`
- `docs/ZAICODE_BASELINE_RECEIPT.md`
- `docs/ZAICODE_ACCOUNT_INDEPENDENCE_VALIDATION.md`
- `docs/ZAICODE_UPSTREAM_DELTA.md`

If `docs/ZAICODE_IMPLEMENTATION.md` is still part of the current source contract, restore/update it as well.

UI.md must remain the authoritative ZAICODE product/workflow contract and must not claim SAIFREN/SAIRoute/9router are implemented.

Architecture documentation must distinguish:

- persisted configured route;
- selected routing backend;
- final resolved provider/model;
- runtime execution identity;
- fallback/failure classification.

UPSTREAM DELTA

Extend `docs/ZAICODE_UPSTREAM_DELTA.md` for every newly touched upstream-owned file.

Prefer:
- new ZAICODE-owned wrapper/modules;
- centralized switches;
- additive schemas;
- existing service/runtime boundaries.

Avoid:
- scattered product-name substitutions;
- renaming upstream protocols/packages;
- copying provider infrastructure;
- duplicating task/session state machines.

MILESTONE 5 — REGRESSION / RECOVERY TEST EXPANSION

Current focused tests cover queue and routing primitives, but the newly identified integration seams need direct tests.

Add focused coverage for:

A. TOOL POLICY
- allowlist propagation;
- denylist propagation;
- plan mode propagation;
- empty policy;
- conflict semantics.

B. ROUTING
- backend persistence;
- default direct migration;
- direct execution;
- unimplemented backend fail-closed;
- configured route vs actual execution identity.

C. PRODUCT IDENTITY
- ZAICODE build environment resolves ZAICODE flavor;
- normal production remains production;
- normal test remains preview;
- malformed identity switch remains rejected;
- canonical ZAICODE build path cannot silently resolve to production/preview identity.

D. QUEUE / EXECUTION REGRESSIONS
Preserve existing coverage for:
- atomic dispatch;
- duplicate dispatch rejection;
- cancellation;
- retry lineage;
- stale completion rejection;
- crash/restart stale-running reconciliation;
- bounded child delegation;
- malformed persisted rows.

Run the existing ZAICODE test suites plus newly added focused tests.

Then run applicable:
- `pnpm typecheck`
- `pnpm lint`
- architecture check for changed boundaries;
- web build if shared/UI contracts changed;
- desktop build;
- ZAICODE desktop smoke.

Do not hide new lint errors inside the inherited warning baseline.

MILESTONE 6 — ACCOUNT-INDEPENDENCE RECHECK

Re-run the no-account contract after the build/identity changes.

Fresh isolated ZAICODE profile must:

1. start without Z.ai login;
2. reach the application shell;
3. open workspace/project state;
4. open Settings;
5. expose agent management;
6. expose work queue;
7. remain valid with zero providers;
8. show a truthful no-route state;
9. contain no mandatory Connect / Upgrade / Coding Plan / Start Plan path;
10. keep Z.ai only as an optional provider integration.

Do not delete optional Z.ai provider support merely to satisfy product-level account independence.

MILESTONE 7 — STANDALONE COEXISTENCE GATE

Acceptance items 4 and 15 from SRC-004 were not fully demonstrated by the stored receipt.

The log itself says packaged installer and interactive concurrency remained manual.

Therefore do not describe these as fully proven until verified.

When environment permits:

1. build/package ZAICODE through the new canonical entrypoint;
2. launch installed/current production ZCode;
3. launch ZAICODE at the same time;
4. confirm no single-instance ownership collision;
5. confirm separate userData;
6. confirm separate settings/database/auth-cookie/log roots;
7. confirm production ZCode profile timestamps/content remain unchanged by ZAICODE;
8. confirm ZAICODE restart preserves only ZAICODE state.

Record exact evidence.

If this needs the operator's real installed desktop environment, preserve it as the final operator-required gate.

T-9 POLICY

T-9 remains BLOCKED until a real interactive desktop E2E is actually run.

Do not close T-9 based on:
- source inspection;
- service unit tests;
- screenshots from another state;
- assumed Electron behavior.

Required real flow remains approximately:

agent create
→ queue create
→ dispatch
→ actual runtime execution
→ terminal queue state
→ inspect result/error
→ restart/coexistence sanity

A configured execution provider and real desktop session may be required.

If all autonomous work is complete and T-9 is the only remaining blocker, stop with exact operator steps and expected observations.

T-10 POLICY

DO NOT IMPLEMENT T-10 in this continuation.

T-10 is intentionally blocked because exposing queue creation as an LLM-callable self-delegation tool creates a new side-effectful autonomy boundary.

The operator has not yet supplied policy for:
- which roles may spawn jobs;
- maximum child jobs;
- budget/cost limits;
- concurrency interaction;
- workspace boundaries;
- tool visibility;
- cancellation inheritance;
- recursion/depth rules beyond the current queue-owned bounded delegation.

Keep T-10 blocked.

Do not work around the blocker by silently giving Coordinator an equivalent unrestricted tool.

NON-GOALS

Do not:
- implement full SAIFREN protocol;
- implement full SAIRoute protocol;
- implement full 9router protocol;
- create autonomous recursive swarms;
- add arbitrary DAG orchestration;
- replace the ZCode task/session runtime;
- create another frontend;
- broadly delete Z.ai provider code;
- redesign unrelated upstream ZCode commercial/UI surfaces;
- modify production ZCode profile;
- weaken existing tests or validators to obtain green gates.

ACCEPTANCE

This continuation is complete when all autonomous items below are true:

1. Current live-tree truth is reconciled with SAIPEN receipts.
2. Mandatory product/architecture contracts exist in the actual source tree.
3. Agent tool-policy fields are either genuinely enforced or unsupported/dead fields are removed from the product contract.
4. Routing backend intent is persistable and reaches the routing resolver.
5. Unimplemented routing backends fail closed.
6. Direct provider/model routing still works.
7. ZAICODE has one canonical development/build/package lane.
8. ZAICODE product mode and ZAICODE identity cannot accidentally diverge through the canonical lane.
9. Existing production/preview ZCode build behavior is not regressed.
10. Account-independent startup remains intact.
11. Zero-provider state remains valid.
12. Existing durable queue invariants remain green.
13. Focused ZAICODE tests pass.
14. Typecheck passes.
15. Lint introduces no new errors.
16. Applicable architecture/build checks pass.
17. Desktop smoke passes where executable.
18. Upstream delta ledger is current.
19. T-10 remains honestly blocked.
20. T-9 is either genuinely verified or remains honestly operator-blocked.
21. No unknown/manual result is converted into PASS.

FINAL STATE RULE

Do not return the project to DONE merely because all code-editable work finished.

If standalone installed coexistence or interactive E2E still requires the operator, end in the protocol-valid blocked/review state with:
- autonomous work complete;
- exact remaining external gate;
- exact command/steps;
- expected observations;
- no fake success receipt.

NEXT EXACT ACTION

Run SAIPEN recovery against the live project and reconcile the missing mandatory artifacts first.

Then repair the highest-severity runtime truth defect:
make ZAICODE agent tool policy actually constrain the runtime execution surface.

After that:
connect persisted routing backend intent,
establish the canonical ZAICODE identity/build lane,
re-run product/account validation,
and drive all remaining autonomous gates to closure before stopping at the genuine interactive operator gate.

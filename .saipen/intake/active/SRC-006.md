ZAICODE

MODE

AUTONOMOUS AUDIT RECONCILIATION + T-12 CLOSURE + NEXT ELIGIBLE WORK

This handoff supersedes narrower continuation instructions for the current ZAICODE state while preserving all valid requirements and invariants from SRC-001 through SRC-005.

Work autonomously through all eligible milestones.

Do not ask the operator to continue between normal milestones.

Do not reopen already-proven implementation merely because SAIPEN STATE currently says BUILD.

Do not stop after reporting findings if an eligible corrective action can be completed safely.

Stop only when:

* all autonomous eligible work is complete;
* only genuine operator/external blockers remain;
* a destructive or policy-sensitive decision requires the operator;
* live source contradicts this handoff in a way that cannot safely be reconciled.

DEFAULT EXECUTION BEHAVIOR

Use maximum practical reasoning depth.

Prefer direct source inspection and executable evidence over summaries or stale graph/index data.

After every bounded implementation or reconciliation slice:

1. inspect exact current source;
2. verify focused behavior;
3. inspect adjacent regression risk;
4. update SAIPEN through the canonical repository protocol;
5. continue automatically to the next eligible phase/work item.

Before context exhaustion:

* write an exact checkpoint;
* record current ticket/phase;
* record files changed;
* record tests already passed;
* record unresolved findings and blockers;
* record the next exact command/action;
* preserve sufficient evidence for cold recovery;
* continue from the checkpoint in the next context without requiring the operator to reconstruct history.

SOURCE BINDING

Project: ZAICODE

Observed audit snapshot:

`_ZAICODE_23.09.26-T18-04-10.zip`

AUDAPACK created:

`2026-09-23T18:04:42`

Recorded source path:

`V:\___VAC\__K\__CODE\_AI_STUFF_AGENTIC\_ZAICODE`

Archive semantics:

`audit_representation`

The archive is not authoritative proof that every live-tree file is absent. AUDAPACK reports substantial filtering/exclusion, and several previously recorded ZAICODE acceptance artifacts are absent from this snapshot.

Always inspect the live project before classifying a missing file as deleted.

CURRENT MACHINE TRUTH FROM THE SNAPSHOT

`.saipen/STATE.md`:

* phase: BUILD
* task: T-12
* next_action: `PHASE BUILD T-12`
* blocker: none
* execution_intent: converge
* converge_target: done
* last_event: E-156
* last recorded action: canonical stop checkpoint

`.saipen/BOARD.md`:

DOING:

* T-12 P1 ZAICODE / SRC-005

DONE:

* T-1
* T-2
* T-3
* T-4
* T-5
* T-6
* T-7
* T-8
* T-11

BLOCKED:

* T-9: real interactive desktop E2E / configured provider / operator desktop session
* T-10: operator policy decision for LLM-callable self-delegation

No ordinary TODO tickets are currently present.

CRITICAL STATE INTERPRETATION

Do not interpret STATE=BUILD as meaning the T-12 implementation is unfinished.

The stored log shows that T-12 already completed the main implementation and passed consolidated VERIFY.

Relevant recorded progression:

E-137:
red control proved the editor dropped existing allowedTools/disallowedTools.

E-138:
tool-policy editor preservation fixed; focused green controls recorded.

E-139:
red control proved routing backend was not persisted and invalid backend input was accepted.

E-140:
additive migration `0005_zaicode_routing_backend` implemented; backend CRUD/default/migration validation recorded.

E-141:
backend selector + Inspector runtime/configured identity separation implemented.

E-142:
product documentation reconciled.

E-143:
canonical ZAICODE environment/identity lane verified.

E-144:
focused regression matrix recorded green.

E-145:
lint remained at inherited baseline: 70 warnings, zero errors, zero new warning signatures.

E-146:
web build and canonical `build:zaicode` passed.

E-147:
canonical bundle and packaged-profile smoke evidence recorded through detail_ref.

E-148:
BUILD described as complete.

E-149:
transitioned to VERIFY.

E-150:
VERIFY PASS with high confidence.

Recorded matrix at E-150:

* services: 17/17
* UI: 4/4
* desktop: 10/10
* canonical identity: 5/5
* ZAICODE env check: PASS
* typecheck: PASS
* architecture check: PASS
* lint: 70 inherited warnings / 0 errors / no new warnings
* web build: PASS
* `build:zaicode`: PASS
* bundle/package path: recorded PASS
* isolated packaged executable smoke: recorded PASS

E-151:
transitioned to REVIEW.

E-152:
REVIEW pass 1/2 found only two documentation-accuracy defects:

1. `docs/ZAICODE_UPSTREAM_DELTA.md` used workspace-relative `zcode/package.json` while neighboring entries were repository-relative.
2. `docs/ZAICODE_ACCOUNT_INDEPENDENCE_VALIDATION.md` carried a stale 10/10 agent/service statement instead of the current 17/17 service matrix.

E-153:
routed back to BUILD only to fix those documentation defects.

E-154 / E-155:
SAIPEN generation/home migration and rebind.

E-156:
stop checkpoint.

IMPORTANT NEW AUDIT OBSERVATION

The current audit snapshot already contains both corrections from REVIEW pass 1:

`docs/ZAICODE_UPSTREAM_DELTA.md`

The relevant ledger row now uses:

`package.json`

not:

`zcode/package.json`

`docs/ZAICODE_ACCOUNT_INDEPENDENCE_VALIDATION.md`

The Agent Management entry now states that the focused services matrix is:

`17/17`

not the stale 10/10 claim.

Therefore the most likely live state is:

* REVIEW pass 1 findings were edited;
* the fixes were not subsequently checkpointed/verified/reviewed in SAIPEN before E-156;
* STATE remains BUILD because protocol closure lagged behind source edits.

Do not blindly re-edit these lines.

MILESTONE A — COLD RECOVERY AND SOURCE/TRUTH RECONCILIATION

Run the canonical SAIPEN recovery/continue path first.

Confirm:

* project lineage;
* T-12 remains the active work item;
* live `.saipen/STATE.md`;
* live `.saipen/BOARD.md`;
* live `.saipen/LOG.md`;
* SAIPEN home binding;
* source tree location;
* current working-tree delta;
* whether the two E-152 documentation fixes already exist in the live source.

If both fixes exist:

* treat them as an unrecorded/post-review BUILD delta;
* do not rewrite them just to create activity;
* checkpoint their exact current contents;
* verify them;
* proceed toward REVIEW pass 2.

If one or both fixes do not exist:

* apply only the missing E-152 correction;
* do not broaden the patch;
* verify;
* checkpoint;
* proceed toward REVIEW pass 2.

Do not create a new ticket merely to record these two fixes unless the SAIPEN protocol itself requires a distinct ticket.

They belong to T-12 / SRC-005.

MILESTONE B — RECONCILE THE MISSING ACCEPTANCE ARTIFACTS

SRC-005 explicitly identified this contradiction and it still exists in the supplied audit representation.

The current archive does NOT contain:

* `UI.md`
* `docs/ZAICODE_ARCHITECTURE.md`
* `docs/ZAICODE_IMPLEMENTATION.md`

Yet `.saipen/LOG.md` repeatedly claims these files existed and were used during previous verification.

This audit snapshot also does not carry the `.saipen/evidence` files referenced by multiple log events.

Inspect the LIVE tree.

CASE A — FILES EXIST LIVE

If the three ZAICODE contract files exist in the live project:

* do not reconstruct them;
* do not rewrite them merely because the audit snapshot omitted them;
* inspect them for current T-12 accuracy;
* record that the current AUDAPACK audit representation again omitted authoritative ZAICODE product/architecture artifacts;
* classify this as an audit-representation provenance/fidelity defect rather than a ZAICODE source deletion;
* preserve the exact filenames and live-tree existence proof in the checkpoint;
* ensure ZAICODE acceptance does not falsely claim that the archive itself carried the artifacts.

Do not repair AUDAPACK from inside ZAICODE unless the currently active project protocol explicitly owns such cross-project work.

CASE B — FILES ARE ACTUALLY ABSENT LIVE

If they are genuinely absent from the live project:

this is a P0 acceptance/reproducibility contradiction.

Do not close T-12.

Reconstruct only the missing authoritative documents using:

* current implementation;
* SRC-001 through SRC-005;
* existing ZAICODE docs;
* current source behavior;
* existing verified receipts.

Do not invent historical evidence.

Document current behavior, not imagined intent.

`UI.md` must remain the ZAICODE product/workflow contract.

`docs/ZAICODE_ARCHITECTURE.md` must describe the actual current architecture.

If `docs/ZAICODE_IMPLEMENTATION.md` remains part of the active contract, restore it accurately.

MILESTONE C — REVIEW PASS 2 OF T-12

After the E-152 fixes are present and verified, perform the required second independent review.

This is a real review, not a ceremonial transition.

Review T-12 against SRC-005 acceptance with emphasis on the changed integration boundaries.

Check at minimum:

TOOL POLICY

Confirm the actual execution path, not only schema/UI persistence.

Current source snapshot shows:

`packages/desktop/src/host/zaicodeRunDispatch.ts`

projects:

* `allowedTools` -> runtime `toolAllowlist`
* `disallowedTools` -> runtime `toolDenylist`
* deny wins over allow on conflict
* plan permission mode -> runtime plan mode

Confirm the restriction reaches the actual first runtime turn through `sendPrompt`.

Also inspect the existing `createTask` cast carefully.

The current source passes `toolAllowlist` into `createTask` through a type cast even though the visible `IZCodeTaskService.createTask` interface does not advertise tool allow/deny fields.

Do not treat the unsupported `createTask` property as enforcement proof.

The authoritative proof must be the actual runtime prompt/session path.

If `sendPrompt` is sufficient for ZAICODE's single dispatched execution turn and the real runtime consumes the restrictions, document that clearly.

If the cast is unnecessary/deceptive but behavior is otherwise correct, either remove it as a bounded cleanup or explicitly classify it as non-authoritative plumbing.

Do not turn this into a second permission engine.

ROUTING

Confirm:

* persisted `backend`;
* migration default = `direct`;
* valid backend validation;
* create/update/read/list/duplicate preservation;
* executor passes `agent.backend` to `resolveZaicodRoutePlan`;
* Direct uses the configured provider/model selection;
* SAIFREN / SAIRoute / 9router remain fail-closed;
* unresolved routes cannot silently fall back to Direct;
* Inspector distinguishes configured route from resolved execution identity.

BUILD / PRODUCT IDENTITY

Confirm canonical scripts remain present:

* `dev:zaicode`
* `build:zaicode`
* `bundle:zaicode`
* `zaicode:env:check`

Confirm `scripts/zaicode-env.mjs` centrally establishes both:

* `ZCODE_ZAICODE_MODE=1`
* `ZCODE_ZAICODE_IDENTITY=1`

Confirm canonical ZAICODE lane resolves:

* flavor = `zaicode`
* appId = `dev.zaicode.app`
* productName = `ZAICODE`
* ZAICODE capability mode enabled

Confirm normal production/preview identities remain unchanged.

Do not replace the upstream build system.

DOCUMENTATION

Confirm:

`docs/ZAICODE_UPSTREAM_DELTA.md`
uses repository-relative `package.json` consistently.

Confirm:

`docs/ZAICODE_ACCOUNT_INDEPENDENCE_VALIDATION.md`
uses the current 17/17 service matrix where appropriate and does not transform historical 10/10 results into retroactive 17/17 claims.

Historical receipts must remain historical.

Current continuation evidence may describe the current matrix separately.

Confirm docs do not claim:

* interactive UI PASS when only code/unit tests were run;
* installed-app coexistence PASS when only structural isolation/package smoke exists;
* SAIFREN implementation;
* SAIRoute implementation;
* 9router implementation;
* real provider execution if no real-provider E2E was performed.

MILESTONE D — FOCUSED REGRESSION REVALIDATION

If REVIEW pass 2 finds no code change beyond the two documentation corrections, do not waste hours rebuilding the entire universe without cause.

Run enough focused verification to prove the post-review delta is sound and that no source drift occurred.

At minimum re-run the cheapest authoritative focused gates available for:

* documentation consistency;
* ZAICODE environment identity;
* T-12 focused test matrix if dependencies are ready and runtime cost is reasonable.

If code changed during REVIEW pass 2, re-run the complete affected matrix.

Expected comprehensive matrix when required:

SERVICES

* agent migration/persistence;
* backend validation;
* agent CRUD;
* duplicate;
* tool-policy update and explicit clear;
* atomic/single-flight dispatch;
* cancellation;
* retry lineage;
* stale completion rejection;
* restart reconciliation;
* bounded child delegation;
* routing;
* malformed persisted state handling.

Expected previously recorded count:

17/17

UI

* editor preservation of allow/deny;
* permission-mode update without erasing policy;
* backend selector behavior;
* truthful backend status.

Expected previously recorded count:

4/4

DESKTOP

* tool policy runtime projection;
* Direct routing;
* external backend fail-closed;
* remote workspace ZAICODE service resolution;
* truthful unavailable behavior.

Expected previously recorded count:

10/10

IDENTITY

* ZAICODE lane resolves correctly;
* missing/malformed identity assertions fail;
* production identity unchanged;
* preview identity unchanged.

Expected previously recorded count:

5/5

GLOBAL GATES WHEN CODE CHANGED OR SOURCE DRIFT REQUIRES THEM

* `pnpm typecheck`
* architecture check for changed boundaries
* `pnpm lint`
* compare warning baseline
* `pnpm --filter @zcode/web build` if shared/UI contracts changed
* `pnpm build:zaicode`
* `pnpm bundle:zaicode` when package/build identity was touched
* isolated packaged smoke when package/build identity was touched

Lint acceptance remains:

* zero errors;
* no new warnings above the inherited 70-warning baseline.

Do not weaken tests or validators to obtain green gates.

MILESTONE E — T-12 SHIP / CLOSURE

If REVIEW pass 2 is clean:

* record REVIEW pass 2;
* transition through the canonical SHIP/closure path;
* close T-12 with `closure_mode: own_patch` if that remains the correct protocol classification;
* preserve SRC-005 linkage;
* record exact tests/evidence used;
* record the audit-representation omission separately from source truth;
* make STATE truthfully reflect the next eligible work.

Do not leave T-12 in BUILD after a clean second review.

Do not mark T-12 DONE if mandatory live-tree artifacts are actually absent.

MILESTONE F — POST-T-12 ELIGIBILITY

After T-12 closes, inspect the board again.

Expected board tail from this snapshot:

T-9 BLOCKED

Reason:

real interactive desktop E2E still requires a configured provider and an operator desktop session.

Do NOT close T-9 using:

* source inspection;
* unit tests;
* build success;
* packaged smoke;
* screenshots from another state;
* assumptions about Electron behavior.

Required real flow remains approximately:

create/save agent
-> confirm provider/model/backend persistence
-> create queue job
-> dispatch
-> actual provider/model runtime execution
-> observe terminal queue status
-> inspect result/error and resolved runtime identity
-> restart
-> verify persistence
-> optionally run production ZCode and ZAICODE concurrently
-> verify no mutable-state collision

If T-9 is the only remaining executable goal and the current agent truly cannot perform the interactive desktop/provider gate, preserve it as BLOCKED and emit exact operator steps.

T-10 BLOCKED

Do not implement T-10 in this continuation.

T-10 introduces an LLM-callable side-effectful delegation boundary.

The operator has not yet specified policy for:

* allowed spawning roles;
* child-job limits;
* cost/budget limits;
* concurrency interaction;
* workspace boundaries;
* tool visibility;
* cancellation inheritance;
* recursion/depth rules.

Do not bypass this decision by exposing an equivalent unrestricted queue-creation tool under another name.

If T-12 is closed and only T-9/T-10 remain blocked, the correct autonomous result is a clean stop with explicit blockers.

DO NOT INVENT EXTRA WORK JUST TO KEEP THE AGENT BUSY.

INVARIANTS

Preserve all of the following:

* production ZCode profile remains untouched;
* ZAICODE development/runtime state remains isolated;
* no account wall is introduced;
* zero-provider ZAICODE remains a valid state;
* Z.ai remains optional rather than being deleted wholesale;
* Direct remains the only implemented ZAICODE routing backend unless separately authorized;
* SAIFREN / SAIRoute / 9router remain truthful fail-closed boundaries;
* configured provider/model/backend must not be displayed as resolved runtime identity unless resolution actually occurred;
* queue state remains authoritative and durable;
* duplicate dispatch remains rejected;
* stale completion remains rejected;
* retry lineage remains intact;
* crash/restart reconciliation remains intact;
* bounded delegation remains bounded;
* no second session/task state machine;
* no duplicate provider infrastructure;
* no broad upstream rename wave;
* no weakening of upstream runtime safety restrictions;
* no fabricated test PASS;
* no manual/unknown result converted into PASS;
* no modification of production credentials/account data;
* no undocumented destructive migration.

NON-GOALS

Do not:

* implement SAIFREN networking;
* implement SAIRoute networking;
* implement 9router networking;
* implement recursive autonomous swarms;
* implement arbitrary DAG orchestration;
* implement T-10 without operator policy;
* replace ZCode task/session runtime;
* create a second frontend;
* redesign unrelated upstream commercial UI;
* remove optional Z.ai provider support;
* broadly rewrite already-working T-12 code;
* recreate missing docs without first checking the live tree;
* treat the audit ZIP as a byte-perfect complete source checkout;
* modify AUDAPACK project source from inside ZAICODE unless separately authorized.

EXPECTED FINAL STATE

Preferred result:

T-12:
DONE after verified E-152 documentation corrections and REVIEW pass 2.

T-9:
BLOCKED honestly on real interactive/provider E2E unless the environment now permits genuine execution.

T-10:
BLOCKED intentionally on operator autonomy-policy decision.

STATE:
no fabricated eligible work.

AUDIT REPRESENTATION:
explicitly notes whether `UI.md`, `docs/ZAICODE_ARCHITECTURE.md`, `docs/ZAICODE_IMPLEMENTATION.md`, and referenced `.saipen/evidence` exist in the live project but were omitted from the supplied AUDAPACK snapshot.

FINAL REPORT FORMAT

Return a compact closure report containing:

STATUS
Current SAIPEN phase/task and whether T-12 closed.

SOURCE TRUTH
Whether the mandatory ZAICODE contract files exist live and whether the audit snapshot omitted them.

REVIEW
Result of REVIEW pass 2 and exact findings, if any.

IMPLEMENTATION
Only changes actually made in this continuation.

VALIDATION
Exact focused/global gates executed and their results.

BLOCKERS
T-9 / T-10 or any newly proven blocker.

REGRESSION RISK
Only concrete residual risks.

CLOSURE
Exact tickets closed/blocked and closure mode.

NEXT EXACT ACTION
The single next protocol action, or `NONE — no autonomous eligible work remains`.

Do not ask "continue?" when another eligible autonomous action exists.

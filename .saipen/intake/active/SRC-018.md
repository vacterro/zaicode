# User request

priority: P1
verify: the requested change is present and demonstrated against the user own description of it

## Request

ZAICODE: SAIMAIL discoverability when unconfigured, GLM off by default with fallback to SAIRoute/SAIFREN, replace remaining ZCode names (tray) with ZAICODE
